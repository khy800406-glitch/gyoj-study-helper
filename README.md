# 교재 공부 도우미

학생 교재 PDF를 올리면 텍스트를 추출하고, 읽어 주거나 핵심을 요약하고, 퀴즈로 점검하는 Streamlit 앱입니다.

로컬 실행:

```bash
python3 -m venv .venv
.venv/bin/python -m pip install -r requirements.txt
.venv/bin/streamlit run app.py
```
